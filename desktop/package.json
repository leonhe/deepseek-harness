{
  "name": "dsh-desktop-poc",
  "version": "0.1.0",
  "private": true,
  "description": "POC Electron shell for the DeepSeek Harness Web GUI (route A): spawns the dsh web host on loopback and hosts the GUI in a native macOS window. Standalone folder, deliberately NOT a pnpm workspace member (root pnpm-workspace.yaml does not include it).",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "smoke": "DSH_DESKTOP_SMOKE=1 electron .",
    "dist:mac": "electron-builder --mac",
    "dist:mac:arm64": "electron-builder --mac --arm64",
    "dist:mac:x64": "electron-builder --mac --x64",
    "predist:mac": "node scripts/bundle-host.mjs",
    "predist:mac:arm64": "node scripts/bundle-host.mjs"
  },
  "devDependencies": {
    "electron": "^42.2.0",
    "electron-builder": "^26.15.7"
  }
}
